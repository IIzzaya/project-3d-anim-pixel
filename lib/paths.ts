/**
 * Deployment base path — the site is served from
 * https://iizzaya.github.io/project-3d-anim-pixel/ on GitHub Pages.
 *
 * vinext's `basePath` config rewrites framework URLs (`/_next/...`, Link,
 * Router) but NOT plain references to files in `public/`. Every public asset
 * referenced from JSX or CSS must therefore be prefixed with this constant.
 */
export const BASE_PATH = '/project-3d-anim-pixel';
